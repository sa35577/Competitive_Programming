#include <bits/stdc++.h>
using namespace std;

int main() {
    int g;
    cin >> g;
    int a;
    int arr[g];
    for (int i = 0; i < g; i++) {
        cin >> a;
        arr[i] = a;
    }
    return 0;
    vector<int> zeros;
    for (int i = 0; i < g; i++) {
        if (lst[i] = 0)
            zeros.push_back(i);
    while (zeros.size() > 0) {
        for (int i = 0; )
    }
}


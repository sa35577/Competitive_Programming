#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.sync_with_stdio(0); cin.tie(0);

    //int a;
    //cin >> a;
    /*
    if (a % 2 == 0) {
        cout << "It is even!" << endl;
    }
    else {
        cout << "It is odd!" << endl;
    }

    int n = 0;
    while (n < 10) {
        n++;
        cout << n;
    }
    */
    /*
    int a = 2;
    // and - &&, or - ||
    if (a > 0 && a % 2 == 0) {
        cout << "Yes!";
    */

    for (int i = 0; i < 10; i++) {
        cout << "hixa" << endl;
    }
    return 0;
}

/*
        integers                   decimals                 "string" - an array of characters, USE double quotes
-2^31 <= int <= 2^31 - 1          float                     'char' - a number
-2^15 <= short <= 2^15 - 1        double                    bool - true and false
-2^63 <= long long <= 2^63 - 1    long double


*/

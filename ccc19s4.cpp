#include <bits/stdc++.h>
using namespace std;


int main() {
    int N,K;
    cin >> N >> K;
    int a[N];
    for (int i = 0; i < N; i++) {
        cin >> A[i];
    }
    int rem = N - (N % K);
    pair<int,int> arr[N+5];
    //first is rem, second is 
    arr[0] = make_pair(0,0);
    bool impossible;
    for (int i = 1; i <= N; i++) {
        if (i <= K) {
            for (int j = i-1; j >= 0; --j) {
                if (arr[j].second+1 < K)
            }    
        }
    }



}
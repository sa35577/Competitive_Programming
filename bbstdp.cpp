#include <bits/stdc++.h>
using namespace std;

int main() {
    unsigned int k =  5, b = 9;
    unsigned int c= k | b;
    cout << c;
    /*int n;
    cin >> n;
    string s;
    cin >> s;
    deque<char> d;
    for (int i = 0; i < s.size(); i++) {
        if (i == 0) d.push_front(s[i]);
        else {
            if (s[i] > d.front()) {
                d.push_back(s[i]);
            }
            else d.push_front(s[i]);
        }
    }
    for (char c : d) {
        cout << c;
    }*/
}